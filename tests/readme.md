# Tests of PsychoPy Standalone (legacy) app

Note that this app is scheduled for deprecation, with the upcoming Electron application 
[PsychoPy Studio](https://github.com/psychopy/psychopy-studio), scheduled to replace it.
As such the test suite here is deliberately short, just checking that the app can be
compiled and launched.